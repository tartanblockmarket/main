# Intentionally left without values.
# Local development should use .env.local.
# New developers can start from .env.local.example.
# Hosted environments should use the platform's environment variable settings.
